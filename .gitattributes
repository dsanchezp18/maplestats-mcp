# Normalize every text file to LF in the repository, whatever the
# committer's OS or core.autocrlf setting. The tree had drifted to a
# mix of CRLF and LF files.
* text=auto eol=lf

# PowerShell scripts are run on Windows; keep CRLF in the working tree.
*.ps1 text eol=crlf
